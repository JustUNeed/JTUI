<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:jt="clr-namespace:JTUI.Controls"
    xmlns:folder="clr-namespace:JTUI.Controls.FolderBin"
    xmlns:img="clr-namespace:JTUI.Controls.ImageGrid"
    xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel">

    <img:HalfThicknessConverter            x:Key="JT.HalfThicknessConverter"/>
    <folder:SizeFromWidthHeightConverter       x:Key="JT.SizeFromWHConverter"/>

    <!-- 单个文件夹格子:条形长方形,文字居中可省略 -->
    <DataTemplate x:Key="JT.FolderItem.Template">
        <Grid>
            <jt:JTButton x:Name="Cell"
                         Content="{Binding DisplayName}"
                         CornerRadius="6"
                         Cursor="Hand"
                         ToolTip="{Binding Path}"
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"
                         IsHitTestVisible="False"/>

            <jt:JTDeleteBadge x:Name="DeleteBtn"
                        Grid.Row="0"
                        Tag="{Binding}"
                        Visibility="Collapsed"/>

        </Grid>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsDropActive}" Value="True">
                <Setter TargetName="Cell" Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="#3B82F6" BlurRadius="10" ShadowDepth="0" Opacity="0.9"/>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"/>
                    <Condition Binding="{Binding ShowDeleteButton, RelativeSource={RelativeSource AncestorType={x:Type folder:JTFolderBin}}}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter TargetName="DeleteBtn" Property="Visibility" Value="Visible"/>
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <Style TargetType="{x:Type folder:JTFolderBin}">

        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="VirtualizingPanel.CacheLength" Value="2"/>
        <Setter Property="VirtualizingPanel.CacheLengthUnit" Value="Page"/>
        <Setter Property="ItemTemplate" Value="{StaticResource JT.FolderItem.Template}"/>

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <vwp:VirtualizingWrapPanel
                        Orientation="Horizontal"
                        SpacingMode="StartAndEndOnly"
                        StretchItems="False">
                        <vwp:VirtualizingWrapPanel.ItemSize>
                            <MultiBinding Converter="{StaticResource JT.SizeFromWHConverter}">
                                <Binding Path="ItemWidth"
                                         RelativeSource="{RelativeSource AncestorType={x:Type folder:JTFolderBin}}"/>
                                <Binding Path="ItemHeight"
                                         RelativeSource="{RelativeSource AncestorType={x:Type folder:JTFolderBin}}"/>
                            </MultiBinding>
                        </vwp:VirtualizingWrapPanel.ItemSize>
                    </vwp:VirtualizingWrapPanel>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin"
                            Value="{Binding ItemSpacing,
                                RelativeSource={RelativeSource AncestorType={x:Type folder:JTFolderBin}},
                                Converter={StaticResource JT.HalfThicknessConverter}}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Grid>
                                    <Border x:Name="Root" Background="Transparent">
                                        <ContentPresenter/>
                                    </Border>
                                    <Border x:Name="Highlight" CornerRadius="6"
                                            BorderThickness="2" BorderBrush="Transparent"
                                            Background="Transparent" IsHitTestVisible="False"
                                            SnapsToDevicePixels="True"/>
                                </Grid>
                       
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type folder:JTFolderBin}">
                    <Border Background="Transparent"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ScrollViewer"
                                  Focusable="False"
                                  CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                  Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>

                            <TextBlock x:Name="EmptyHint"
                                              Margin="8"
                                    Text="拖入任意文件夹创建快速链接"
                               Foreground="{DynamicResource JT.Foreground}"
                               Opacity="0.45"
                               FontSize="13"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="EmptyHint" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>
